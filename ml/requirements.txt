
scikit-learn==1.4.0
xgboost==2.0.3
pandas==2.2.0
numpy==1.26.3
scipy==1.12.0

# Mongo → Parquet archival architecture (archive_snapshots.py,
# compact_parquet.py, parquet_loader.py). pyarrow is required; duckdb is
# optional but strongly recommended — parquet_loader.py falls back to a
# slower pure-pandas glob+concat if duckdb isn't installed.
pyarrow==24.0.0
duckdb==1.5.4

pymongo==4.6.1
motor==3.3.2          # async MongoDB driver

fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3

apscheduler==3.10.4

httpx==0.26.0
requests==2.31.0

python-dotenv==1.0.0
joblib==1.3.2          # model serialization

loguru==0.7.2

statsmodels==0.14.1
